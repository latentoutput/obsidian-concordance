Marker-only index whose block is broken. Not named like an index, so before
the malformed fix this note vanished from every report.

%% concordance:end %%
%% concordance:start mode="folder" folder="Concordance/Docs" %%
